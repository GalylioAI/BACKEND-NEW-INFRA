/** Max width for wide glass showcase strips (parapharmacie, electronics). */
export const SHOWCASE_SECTION_MAX_WIDTH = 1680;

/** Horizontal gutter on outer showcase wrappers (matches parapharmacie). */
export const SHOWCASE_SECTION_GUTTER_PX = 20;
